## 项目概况

本项目为基于 Unity 开发的 2D 游戏项目，主要使用 UGUI 搭建界面系统。项目中接入了多个常用插件与框架，包括 YooAsset、Newtonsoft.Json、ES3、DOTween 等。

开发时应优先遵循 Unity 2D 项目的结构和性能习惯，避免引入不必要的 3D 逻辑、复杂渲染方案或与当前项目架构不一致的实现方式。

## 技术栈

* 引擎：Unity
* 项目类型：2D
* UI 系统：UGUI
* 资源管理：YooAsset
* JSON 处理：Newtonsoft.Json
* 本地存档：ES3
* 动画/缓动：DOTween

## 开发原则

1. 保持代码清晰、可维护，避免过度设计。
2. 优先复用现有项目结构、命名规范和工具类。
3. 新增功能时应尽量降低对现有系统的侵入。
4. 不随意修改公共框架、插件源码或全局配置。
5. 修改代码前应先理解调用链和资源加载流程。
6. 避免在 Update 中写高频、低必要性的逻辑。
7. UI 逻辑应注意生命周期，避免重复注册事件或产生内存泄漏。

## Unity 相关规范

* 脚本应优先使用 C#。
* MonoBehaviour 中应合理使用 Awake、Start、OnEnable、OnDisable、OnDestroy。
* UI 按钮、事件监听应在合适生命周期中注册与移除。
* 不要随意使用 GameObject.Find、FindObjectOfType 等高开销查找方式。
* 尽量通过序列化字段、依赖注入或统一管理器获取引用。
* 2D 项目中避免引入不必要的 3D 物理、3D 摄像机逻辑或复杂 Shader。
* 对 Prefab、Scene、ScriptableObject 的修改应谨慎，避免破坏已有引用。

## UGUI 规范

* UI 界面逻辑应与业务逻辑适当分离。
* Button、Toggle、Slider 等控件事件需要避免重复绑定。
* 动态列表应考虑对象复用，避免频繁 Instantiate/Destroy。
* Canvas 层级、Sorting Order、Raycast Target 应按现有规则处理。
* UI 动画优先使用 DOTween，不建议手写复杂插值逻辑。
* 关闭界面时应清理定时器、事件、Tween 和异步回调。

## YooAsset 使用规范

* 资源加载应遵循项目已有的 YooAsset 封装方式。
* 不要绕过统一资源管理接口直接加载 AssetBundle。
* 异步加载资源时需要处理失败、取消、界面已关闭等情况。
* 加载得到的资源应按项目规范释放，避免资源泄漏。
* 新增资源路径、资源包名或资源标签时，应保持命名清晰。
* 不要随意修改 YooAsset 初始化、更新、下载流程。

## Newtonsoft.Json 使用规范

* JSON 序列化和反序列化优先使用 Newtonsoft.Json。
* 数据结构应使用清晰的 DTO 或配置类。
* 对外部数据、配置数据反序列化时应做好空值和异常处理。
* 不要假设字段一定存在，尤其是热更新配置、远程配置或旧版本存档。
* 避免在性能敏感路径中频繁进行大量 JSON 解析。

## ES3 使用规范

* 本地存档优先使用 ES3。
* 存档 Key 应统一管理，避免硬编码散落在各处。
* 读取存档时应提供默认值，并兼容旧版本数据，读取string类型的数据时，需要注意重载，第二个参数使用(defaultValue:）显示传参的方式进行赋值。
* 写入存档前应确认数据合法。
* 不要在高频逻辑中频繁写入 ES3。
* 涉及存档结构变更时，应考虑版本兼容和迁移逻辑。

## DOTween 使用规范

* UI 动画、移动、缩放、透明度变化优先使用 DOTween。
* Tween 应在对象销毁、界面关闭或状态切换时正确 Kill。
* 避免对同一对象同时创建多个冲突 Tween。
* 使用 Sequence 时应注意生命周期和回调安全。
* 回调中访问 UI 或 GameObject 前，应确认对象仍然有效。
* 不要依赖未清理的 Tween 维持核心业务状态。

## UIManager 使用规范

* 使用[TransformPath("")]标记对象时，对象的类型不能为GameObject。

## 代码风格

* 类名、方法名、字段名应与项目现有风格一致。
* 公共接口命名应清晰表达用途。
* 私有字段建议使用统一前缀或遵循项目已有规则。
* 方法应保持单一职责，避免过长。
* 复杂逻辑应添加必要注释，但不要写无意义注释。
* 日志输出应适度，避免污染控制台。

## 安全修改范围

可以修改：

* 业务逻辑脚本
* UI 控制脚本
* 配置读取逻辑
* 资源加载调用逻辑
* 存档读写逻辑
* DOTween 动画逻辑

需要谨慎修改：

* 项目启动流程
* YooAsset 初始化流程
* 全局管理器
* 公共基类
* 插件源码
* 构建配置
* Address/资源路径规则
* 存档格式

默认不要修改：

* 第三方插件源码
* Unity ProjectSettings
* Packages 配置
* 已稳定运行的全局框架代码
* 与当前需求无关的大量文件

## 任务处理方式

在执行开发任务时，应遵循以下流程：

1. 先阅读相关脚本、Prefab、配置和调用关系。
2. 判断功能属于 UI、资源、存档、动画还是业务逻辑。
3. 尽量使用项目已有封装完成需求。
4. 修改范围保持最小。
5. 完成后检查是否存在空引用、重复事件、资源未释放、Tween 未清理等问题。
6. 如涉及存档、资源加载或异步逻辑，应考虑异常和兼容情况。

## 常见注意事项

* UI 页面关闭后，异步加载回调可能仍会执行，需要判断对象状态。
* DOTween 动画可能在对象销毁后继续执行，需要主动 Kill。
* ES3 读取旧数据时可能字段缺失，需要默认值保护。
* YooAsset 加载失败时不应导致界面卡死。
* Newtonsoft.Json 反序列化外部数据时需要 try-catch 或安全校验。
* 2D 项目中不要随意加入 3D 摄像机、光照或物理逻辑。
* 不要为了单个需求重构大范围系统。

## 输出要求

当需要生成或修改代码时：

* 给出完整、可直接使用的代码。
* 保持与 Unity C# 风格一致。
* 必要时说明脚本挂载位置和 Inspector 配置。
* 如果涉及 YooAsset、ES3、DOTween，应说明资源释放、存档 Key 或 Tween 生命周期。
* 如果存在不确定的项目封装，应优先标注假设，并尽量写成易于替换的形式。

## 禁止事项

* 不要随意删除已有逻辑。
* 不要擅自替换项目架构。
* 不要绕过现有资源管理系统。
* 不要在未确认用途的情况下修改公共配置。
* 不要引入与项目无关的新插件。
* 不要生成只适用于 3D 项目的实现。
* 不要忽略 Unity 生命周期和对象销毁问题。
